BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "jma-rpi"
BBFILE_PATTERN_jma-rpi := "^${LAYERDIR}/"
BBFILE_PRIORITY_jma-rpi = "6"

